# 变更日志

本项目的所有显著变更将记录在此文件。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.9.0] - 2026-09-11

### Fixed

- **修复卡片视图不显示、切换后表格也无法恢复**：库页的表格/卡片视图切换用了 `<Transition mode="out-in">`，而 DataTable 的模板是 `<table>` + 图片预览 `<Teleport>` 两个根节点——Vue 要求过渡子组件必须单根，多根组件只触发一条警告，实际后果是切换后新视图不渲染、旧视图也回不去，整个切换永久卡死。现在 DataTable 包上单根容器（图片预览的 Teleport 移入容器内），并新增回归测试用真实组件在过渡中反复切换守护该约束。

### Added

- **标题栏悬停提示**：鼠标悬停在控件上时，桌面标题栏中间会显示该控件的用途说明（例如悬停「导入文件」显示「进入向导，导入数据文件，快速建立数据库。」），移开或点击后恢复页面标题；覆盖侧边导航、窗口控件、首页与库页的全部主要按钮，提示与标题之间带快速轻位移切换。
- **侧边栏悬浮化与收起/展开**：桌面侧边栏改为圆角矩形的悬浮卡片（与内容区脱开，带描边与浅投影）；右下角新增收起按钮，点击后侧边栏平滑折叠、内容区铺满，标题栏左侧出现展开按钮可随时恢复。
- **移动端版本号**：移动端顶栏右上角显示当前版本号（v0.9.0），与桌面侧边栏底部的版本号一致。

### Changed

- **标题栏改用纯白底色**：浅色主题下标题栏从暖灰（--bg）改为纯白（--surface），与下方内容背景拉开层次；深色主题仍用对应的表面色。
- **弹窗动效落到面板本体**：模态窗口此前只有遮罩淡入淡出（面板自己不动）；现在遮罩快速淡入淡出，面板本体做轻微上浮缩放 + 材质模糊的非线性运动（进 220ms / 出 150ms，与页面切换同一曲线族），条目抽屉的出场也加速到与弹窗同节奏。全部动画尊重系统「减弱动态效果」设置。

[0.9.0]: https://github.com/mogrowang/collecout/compare/v0.8.0...v0.9.0

## [0.8.0] - 2026-09-11

### Fixed

- **修复数据位置不记忆、默认位置回落 AppData（根因修复）**：此前默认数据位置由前端用 fs 插件按 `baseDir` 逐个探测，Windows 上对 exe 目录的 scope / 路径解析不可靠，探测失败又被静默吞掉，默认根总是悄悄退回 AppData——OOBE 显示错误位置、指针文件随之写错地方，「重启后记住位置」自然失效。现在改由 Rust 命令 `resolve_default_root` 用 `std::fs` 权威解析（exe 目录便携优先 → 家目录 → AppData，结果唯一确定），前端拿到绝对路径后登记进 fs 插件运行时 scope，数据根全部以绝对路径读写；指针文件固定写入真正的默认根，重启后准确恢复上次选择。
- **修复 Windows 任务栏图标发虚（根因修复）**：窗口类图标在注册时被锁在 32px（`LR_DEFAULTSIZE`），高 DPI 任务栏（125–175% 需要请求 30/36/42px）只能从 32px 放大显示，此前调整 `.ico` 档位与编码都绕不开这一层。现在窗口创建后通过 `WM_SETICON` 显式设置 64px 高分辨率图标，运行时生效、不受系统图标缓存影响；`.ico` 的 19 档补齐保留（资源管理器、桌面快捷方式等 shell 场景仍走该文件）。
- **修复「打开数据文件夹」无响应**：opener 插件的 `open_path` / `reveal_item_in_dir` 在插件侧还有独立的路径 scope 校验，即使权限声明齐全也会被静默拒绝。改用自研 Rust 命令直接调用 explorer / open / xdg-open，导出窗口的「在访达中显示」一并受益。
- **修复界面字体无法选择自定义**：选「自定义…」时若输入框为空会立即把设置弹回「跟随系统」，输入框根本不出现。现在选中后稳定停留在自定义输入模式，输入后才持久化；清空输入不再误改设置。

### Changed

- **设置页间距**：同一分组的多个设置行之间新增 12px 行距，自定义字体输入框与下拉之间留出明显间隔，控件不再挤作一团。

[0.8.0]: https://github.com/mogrowang/collecout/compare/v0.7.0...v0.8.0

## [0.7.0] - 2026-09-11

### Added

- **OOBE 可读取现有数据文件夹**：首次启动向导会自动探测旧版本可能留下的数据位置（AppData 里的 `collecout-data` 与 0.1.0 的 `collecout`、家目录的 `collecout-data`），检测到时新增「读取现有数据」选项，选中后原样读取、不移动不复制文件；「选择其他文件夹」也不再强制空文件夹——所选目录是一个有效的萃序数据文件夹时（换电脑接回备份、挪动过的数据等）直接读取，并提示「将读取其中的现有数据」。
- **界面字体设置**：设置页「外观」新增「界面字体」，内置跟随系统 / 宋体（衬线）/ 楷体 / 等宽四个跨平台预设，也可输入任意字体名（如「霞鹜文楷」）自定义，下方示例文字实时反映所选字体；设置即时生效并随设置持久化，下次启动自动恢复。

### Fixed

- **数据位置探测不留空壳**：默认数据位置（exe 目录优先，绿色便携）探测失败时，会把刚创建的空 `collecout-data` 一并清掉，不再在只读位置残留空文件夹。
- **修复 Windows 任务栏图标发虚**：`.ico` 此前只有 16/20/24/32/40/48… 常规档位，而任务栏在高 DPI 下会请求 30px（125%）、36px（150%）、42px（175%）、54/56/60px（225–250%）等中间尺寸，explorer 只能拿最近一档就近缩放，导致图标轻微发糊。现在按任务栏 24 系、大图标 32 系、小图标 16 系把档位补齐到 19 帧（16–128 全 BMP + 256 PNG），每个缩放档都原生渲染，不再缩放采样。

### Changed

- **过渡动画补齐**：导入向导的五个步骤切换、库页表格 ↔ 卡片视图切换、首页空状态 ↔ 库网格、导出窗口预览区的生成与替换，现在都有快速非线性过渡（与页面切换同一曲线，进 150–170ms / 出 90–100ms）；窗口亮出时内容整体轻微上浮淡入，消除「突然出现」的生硬感。全部动画尊重系统「减弱动态效果」设置。

[0.7.0]: https://github.com/mogrowang/collecout/compare/v0.6.0...v0.7.0

## [0.6.0] - 2026-09-11

### Added

- **桌面标题栏回归**：顶部一条完整标题栏——左侧 logo、居中显示当前页面标题（库页显示库名且重命名后即时更新，导入 / 模板 / 设置 / 关于各归其位）、右上角窗口药丸控件收进栏内；按住标题栏空白处即可拖动窗口，双击切换最大化。OOBE 首启向导同样带标题栏。
- **启动即就绪**：窗口改为以隐藏方式启动，待库与设置加载完、首帧渲染好再显示（另有 3 秒兜底强制显示，防止前端异常时窗口不出现），消除启动瞬间的白屏闪烁。
- **导入向导步骤可点按**：已走过的步骤可直接点击跳回查看或修改，当前步与未解锁的步骤置灰不可点。
- **任意空白处框选**：多选启用后，在页面任意空白位置（页头、工具栏旁、卡片间隙）按下拖动即可绘制多选框，不再局限于条目区域；按住 Shift 拖拽框选会在已有选择上追加。

### Changed

- **导入向导底部按钮药丸化**：「上一步 / 下一步 / 完成」改为最大圆角的药丸造型，与底部悬浮药丸呼应。
- **库页工具栏重排**：「多选 / 筛选 / 排序」三个按钮移到「表格 / 卡片」视图切换之后，计数与「清除筛选」随后；选中状态与搜索框仍靠右。
- **库页条目区独立滚动**：表格与卡片改为在自身容器内滚动，拥有独立的垂直与水平滚动条（表格按内容自然宽度展开，列多时出现水平滚动条）；页面标题、存放位置与工具栏固定不再随页面滚走，切换库后滚动位置自动回到顶部。
- **材质做减法**：标题栏与库页工具栏不再悬浮于内容之上，玻璃材质随之撤去（没有内容从其下穿过，材质失去意义）；导入向导底部悬浮药丸保留材质。

[0.6.0]: https://github.com/mogrowang/collecout/compare/v0.5.0...v0.6.0

## [0.5.0] - 2026-09-11

### Added

- **手动添加条目**：库页面右上角新增「添加条目」按钮，用条目抽屉直接录入信息，不再依赖导入文件。
- **导出为 Excel（.xlsx）**：一行一条的电子表格导出（零依赖 OOXML 生成器），单元格图片以锚定图片的形式嵌入对应字段单元格，表头加粗。
- **全格式导出携带单元格图片**：Word 在对应字段下内嵌图片；PDF / 图片导出把图片随字段绘制进页面；Markdown / 纯文本 / CSV / JSON 写出图片相对路径，并把图片原文件一同导出到文件旁的 `images/` 目录（浏览器预览则逐张下载）。
- **文件夹导出的图片组织方式**：导出为本地文件夹时可选「按条目分行」（每个条目一个文件夹，图片随条目存放）或「按字段分列」（每个字段一个文件夹，收集该列全部图片），图片一律导出原始文件而不是链接；原始文件名随行保留。
- **窗口状态记忆**：桌面端接入 window-state 插件，记住上次关闭时的窗口位置与大小，下次启动自动恢复。
- **Windows 图标修复**：`.ico` 小尺寸（16–128px）改用 BMP 位图编码并补齐 20/40/96 等 DPI 档位——资源管理器与任务栏对 PNG 压缩的小图标兼容差，是此前图标发糊、低清显示的原因；256px 档保留 PNG。

### Fixed

- **修复 PDF / 图片导出信息错位**：字段名与字段值原先按固定 16px 缩进绘制，中文字段名一长就与内容重叠；现在按全部字段名的实际宽度计算统一名称列，值整体右移对齐，长文本按列宽换行。
- **修复设置页「打开数据文件夹」无响应**：能力清单缺少 `opener:allow-open-path` 权限导致打开文件夹的调用被静默拒绝；补上权限（同时补 `allow-open-url`），失败时给出错误提示。文件夹导出后的「在访达中显示」同样受益。
- **修复下拉菜单位置漂移**：自定义下拉组件把菜单钳制在视口内时用了固定宽度估算，触发器贴近视口右缘时菜单会整体漂到左侧；改为按菜单实际宽度定位并右缘对齐（设置页「默认导出格式」最为明显）。
- **修复导入第一步文件列表未对齐**：列表带了浏览器默认的 `<ul>` 缩进；清零并统一图标 / 名称 / 操作列的对齐，同时补充「以下文件将被导入」标题与数量提示。
- **修复本地文件夹导出在移动端 / 浏览器不下载图片的问题**（随图片携带逻辑一并处理）。

### Changed

- **导入向导布局重排**：步骤指示移到「导入」标题之后并靠右对齐；「上一步 / 下一步」固定悬浮于页面底部居中，收进半透明药丸材质（第一步不再显示「上一步」）。
- **导入第 4 步预览表显示单元格图片**：每条草稿在其字段列内展示对应图片缩略图，无列归属的图片进入行尾「图片」列，点击可放大。
- **Excel 导入默认选中第一个工作表**（按工作簿原顺序），不再挑行数最多的表；自动模式选中的表会同步回工作表选择器，保持展示与实际提取一致。
- **库页面工具栏重排**：「多选 / 筛选 / 排序」三个按钮成组靠右，搜索框收在最右侧；左侧保留视图切换与计数。
- **移除桌面端标题栏**：窗口控制（最小化 / 最大化 / 关闭）收进右上角一颗可拖动的半透明药丸，标题栏 logo 一并移除（侧栏品牌保留）；界面整体遵循 Apple 材质规范与反模板化准则梳理。
- **导入第 5 步排版铺满页面**：入库设置卡片不再限制 520px 宽度。

[0.5.0]: https://github.com/mogrowang/collecout/compare/v0.4.0...v0.5.0

## [0.4.0] - 2026-09-10

### Added

- **品牌焕新**：全新图形与文字 logo；应用图标按平台生成（Windows 白底圆角、macOS 规格图标由 macOS 26+ 自动遮罩适配、Android 自适应图标），包名统一为 `com.mws.collecout`。
- **Excel 单元格图片导入**：工作表里插入的图片（浮动图片与 Excel 365「嵌入单元格」两类存储）随导入提取，按所在列归入对应字段——表格 / 卡片 / 条目详情里像文字一样显示缩略图，点击查看原图；同一张图被多个单元格引用时只存一份字节。
- **任意文件导入**：不支持解析的格式作为附件随库存档；源文件默认复制进库文件夹，桌面端可选仅记录原位置。
- **导出格式扩展**：新增 Word（.docx，零依赖 OOXML 生成器）、PDF（A4 分页排版）与图片（每页一张 PNG）；本地文件夹导出可选 .md / .txt 条目格式。
- **多选重做**：工具栏「多选」开关——桌面支持框选与 Ctrl/Shift 点选（不再需要先选中一条），触屏支持左右滑动选择。
- **关于页与检查更新**：设置页新增关于页，通过 GitHub Releases 检查新版本并可直达发布页。
- **导入向导防呆**：图片所在工作表与当前导入的工作表不一致时显示警示提示。

### Fixed

- 修复个人模板无法编辑的问题（`structuredClone` 无法克隆响应式代理导致编辑器停留在内置模板状态）。
- 修复 logo SVG 填充规则（evenodd 在浏览器中把书脊带渲染成多余环形）。
- 修复设置页卡片在窗口拉宽时不再变宽的问题。
- Excel 图片与条目的对应改为结构化行号映射，不再依赖定位文案；工作表不一致时明确提示。
- 需求变化记录：库数据结构升级为「文件夹 + JSON」（`library.json` + `files/` 附件），旧版单文件库与外部单文件库自动迁移。

### Changed

- 主题强调色从墨青改为品牌绿 `#47974F`（暗色为提亮变体）。
- 全部原生下拉替换为自定义下拉组件（锚定展开、键盘导航、非线性动效）；路由切换加入过渡动效。
- 左上角文字 logo 支持点击返回主页；桌面与移动端 logo 高度统一为 28px。
- 设置项卡片控件靠右对齐，主题选项带颜色预览。
- 空库状态改为引导右上角操作；设置图标重绘为标准齿轮；移除触屏点击的蓝色高亮矩形。
- Windows 发布构建隐藏控制台窗口；版本号升至 0.4.0。

[0.4.0]: https://github.com/mogrowang/collecout/compare/v0.3.0...v0.4.0



### Added

- **首次启动引导（OOBE）**：桌面上第一次运行（还没有数据文件夹）时进入欢迎向导，询问数据存放位置，默认跟随软件所在目录而不是系统 AppData；之后可在设置页随时更改（更改时自动把现有数据复制到新位置，原位置保留为备份）。
- **库可独立存放**：新建库时（首页「新建库」与导入向导）可选择把库文件（.json）存到任意文件夹；库页面新增「存放位置」显示与「更改位置」按钮，可随时把库移动到其他位置或移回数据文件夹。外部库通过数据文件夹内的索引登记，所在文件夹不可用时自动跳过。
- **追加入库的重复项检测**：追加导入前先与库中现有条目比对——完全重复的条目默认覆盖更新；标题相同但内容不同的冲突条目会逐条列出差异，可对每条选择「覆盖 / 跳过 / 编辑」，编辑冲突条目时可对照现有值修改导入值后再入库。
- **多选体验重做**：勾选至少一条后进入多选模式——可在表格 / 卡片区拖动框选批量圈选（按住 Ctrl/Cmd 拖拽为追加），勾选框与卡片点击支持 Shift 范围选择与 Ctrl/Cmd 单个切换，新增 Ctrl/Cmd+A 全选、Esc 取消选择；卡片视图同样支持多选。
- **排序面板**：工具栏新增「排序」入口，表格与卡片视图共用；列头点击排序保留，并显示排序方向。
- **纯文本导出排版选择**：导出纯文本时可选「制表符分隔」（首行表头，适合粘贴到表格）或「分节形式」（每条一个小节逐字段罗列）。
- **导出「复制到剪贴板」**：导出窗口底部新增复制按钮，按当前范围 / 字段 / 排版生成完整内容复制到剪贴板，不关闭导出窗口；点击「导出」成功落盘后才自动关闭窗口。
- **自定义标题栏**：桌面端改用自绘标题栏（最小化 / 最大化 / 关闭、拖拽移动窗口），不再使用系统原生标题栏。

### Fixed

- **修复导出为本地文件夹失败**：一是权限清单缺少 `fs:allow-write-text-file`（`write_text_file` 命令从未被授权，AppData 内能写纯属组合权限的副作用）；二是用户选择的目录不在文件系统插件运行时 scope 内——现在通过对话框选择的路径会调用自定义 Rust 命令 `extend_fs_scope` 显式登记（含子目录递归）。

### Changed

- 桌面端数据根目录改为「默认位置 + 指针文件」：指针存放在默认数据文件夹 `collecout-data` 内，记录用户自定义的数据位置；自定义位置失效时启动自动回退默认。0.1.0 旧数据迁移逻辑保留。

[0.3.0]: https://github.com/mogrowang/collecout/compare/v0.2.0...v0.3.0

## [0.2.0] - 2026-09-09

### Fixed

- **修复「入库后信息全无」的严重 bug**：自动识别导入时，条目值按现场推断的字段 id 保存，而库展示时用的内置「自动识别」模板没有任何字段，导致入库后表格、卡片、筛选全部为空。现在每个库自带字段快照，条目值与展示字段永远对得上；0.1.0 的旧库在加载时会自动重建字段表。

### Added

- **表格布局预设**：导入向导里可为每张表选择「自动判断 / 首行为标题栏（每行一条）/ 首列为标题栏（每列一条）」；自动判断会识别「左侧列为字段名」的纵排字段-值表（如个人简历页）并自动转置。
- **多工作表选择**：Excel 的每个工作表都解析为候选表格，导入向导里可切换工作表并实时重新提取；识别结果页会列出全部工作表。
- **追加导入按字段名合并**：向现有库追加导入时，同名字段自动对齐，导入文件里的新列会补充进库的字段表。
- 入库、编辑、删除条目改为即时落盘（不再防抖 400ms），避免意外退出丢数据。

### Changed

- **数据存储位置不再放在系统 AppData**：Windows 存在 exe 所在目录的 `collecout-data` 文件夹（绿色便携，数据跟着程序走）；macOS 存在家目录 `~/collecout-data`（exe 在 .app 包内，写入会破坏应用签名，故不采用）；Linux 先试 exe 目录、失败落回家目录；安卓存在应用外部专属目录（`Android/data/<包名>/files/collecout-data`）。0.1.0 的旧数据会在启动时自动迁移；设置页可查看实际路径并一键打开。
- 设置页的「数据存储位置」现在显示适配器解析出的真实路径。

[0.2.0]: https://github.com/mogrowang/collecout/compare/v0.1.0...v0.2.0

## [0.1.0] - 2026-09-09

### Added

- 首个可用版本：Vue 3 + TypeScript + Vite 前端，Tauri 2 桌面端，Capacitor 7 安卓端。
- 导入 Word（.docx）、Excel / CSV、纯文本（.txt / .md），解析为统一的块结构。
- 本地规则引擎：按模板提取字段（关键词、正则、标题分节、表格列映射），支持中英文日期识别；每个字段带置信度，低置信度内容在导入预览中高亮并可逐格修正。
- 「自动识别」模板：分析文档结构（表格主导 / key:value / 日志式段首日期），现场推断字段与提取模式。
- 模板系统：内置会议纪要、读书笔记、收支流水、通用表格；支持新建自定义模板与复制内置模板，字段名称 / 类型 / 提取方式可配置，附样例试提取。
- 库与条目管理：表格 / 卡片双视图，列排序，全文搜索，按日期时间段、数值区间、标签与包含文字组合筛选。
- 导出：Markdown / 纯文本 / CSV / JSON 单文件导出，本地文件夹导出（每条目一个 Markdown 文件 + 索引.md + index.json）；导出范围支持全部、当前筛选结果与勾选条目，字段可选。
- 存储：桌面端（Tauri plugin-fs）与安卓端（Capacitor Filesystem）各一套 JSON 文件存储，浏览器开发预览走 localStorage。
- 外观：浅色 / 深色 / 跟随系统主题；桌面与移动端双布局。
- 53 个 Vitest 单元测试覆盖解析、提取、筛选与导出。
- `scripts/gen-icon.mjs`：零依赖 Node 脚本程序化生成应用图标。

[0.1.0]: https://github.com/mogrowang/collecout/releases/tag/v0.1.0
